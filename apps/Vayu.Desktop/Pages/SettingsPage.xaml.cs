using Microsoft.UI.Xaml.Controls;

namespace Vayu_Desktop.Pages;

/// <summary>
/// Read-only Settings page for M1. M2/M3 wire AI mode and verbose-log
/// toggles to real bindings.
/// </summary>
public sealed partial class SettingsPage : Page
{
    public SettingsPage()
    {
        InitializeComponent();
    }
}
