<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Vayu_Desktop.Pages.FirstRunSetupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:svc="using:Vayu_Desktop.Services"
    Background="Transparent"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Padding="36,28,36,28" RowSpacing="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header + stepper -->
            <StackPanel Grid.Row="0" Spacing="12">
                <StackPanel Spacing="4">
                    <TextBlock Text="VAYU SETUP DASHBOARD" Style="{StaticResource VayuTitleText}" FontSize="32" />
                    <TextBlock x:Name="StepIndicatorText" Style="{StaticResource VayuSubtitleText}" Text="Step 1 of 5 · Welcome" />
                </StackPanel>
                <!-- Stage stepper. Shows the full onboarding journey so setup reads
                     like a dedicated first-run experience, not a settings page. -->
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Border Style="{StaticResource VayuChipTeal}"><TextBlock Style="{StaticResource VayuChipText}" Text="1 · WELCOME" /></Border>
                    <Border Style="{StaticResource VayuChip}"><TextBlock Style="{StaticResource VayuChipText}" Text="2 · STORAGE" /></Border>
                    <Border Style="{StaticResource VayuChip}"><TextBlock Style="{StaticResource VayuChipText}" Text="3 · AI MODE" /></Border>
                    <Border Style="{StaticResource VayuChip}"><TextBlock Style="{StaticResource VayuChipText}" Text="4 · LOCAL AI" /></Border>
                    <Border Style="{StaticResource VayuChip}"><TextBlock Style="{StaticResource VayuChipText}" Text="5 · ONLINE AI" /></Border>
                    <Border Style="{StaticResource VayuChip}"><TextBlock Style="{StaticResource VayuChipText}" Text="6 · VOICE" /></Border>
                    <Border Style="{StaticResource VayuChip}"><TextBlock Style="{StaticResource VayuChipText}" Text="7 · TRUST &amp; FINISH" /></Border>
                </StackPanel>
            </StackPanel>

            <!-- Step content -->
            <Border Grid.Row="1" Style="{StaticResource VayuGlassCard}">
                <Grid>
                    <!-- 1. Welcome -->
                    <StackPanel x:Name="WelcomePanel" Spacing="10">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Border Width="3" Height="14" Background="{StaticResource VayuAccentCyanBrush}" />
                            <TextBlock Style="{StaticResource VayuSectionText}" Text="WELCOME TO VAYU" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock Style="{StaticResource VayuBodyText}" Text="Vayu is a local-first Windows AI command center. This Setup Dashboard gets you to a working AI in a few steps." />
                        <TextBlock Style="{StaticResource VayuMutedText}" Text="Vayu never installs software or downloads models without your permission. Every step is skippable — Vayu stays usable without it." TextWrapping="Wrap" />
                        <TextBlock Style="{StaticResource VayuMutedText}" Text="What you'll set up: storage, AI mode, Local AI, Online AI, voice, and trust." TextWrapping="Wrap" />

                        <!-- Storage stage (paths default to local app data; editable + persisted). -->
                        <Border Style="{StaticResource VayuGlassCardElevated}" Margin="0,8,0,0">
                            <StackPanel Spacing="8">
                                <TextBlock Style="{StaticResource VayuSectionText}" Text="STORAGE" />
                                <TextBlock Style="{StaticResource VayuMutedText}" TextWrapping="Wrap"
                                           Text="Workspace, logs, cache, models, and generated assets. Defaults live under %LOCALAPPDATA%\Vayu. Set a root and Save — Vayu validates it and creates the folders only after you confirm." />
                                <TextBox x:Name="StorageRootBox" Header="Vayu storage root" />
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <Button x:Name="SaveStorageButton" Content="Save storage paths"
                                            Click="OnSaveStorageClick" Style="{StaticResource AccentButtonStyle}" />
                                    <Button x:Name="ResetStorageButton" Content="Reset to default"
                                            Click="OnResetStorageClick" />
                                </StackPanel>
                                <TextBlock x:Name="StorageMessageText" Style="{StaticResource VayuMutedText}" TextWrapping="Wrap" Text="" />
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- 2. Mode -->
                    <StackPanel x:Name="ModePanel" Spacing="10" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Border Width="3" Height="14" Background="{StaticResource VayuAccentTealBrush}" />
                            <TextBlock Style="{StaticResource VayuSectionText}" Text="PICK AN AI MODE" VerticalAlignment="Center" />
                        </StackPanel>
                        <RadioButton x:Name="ModeOfflineRadio"
                                     Content="Offline only — Ollama + Gemma (recommended)"
                                     IsChecked="True"
                                     Checked="OnModeOfflineChecked" />
                        <RadioButton x:Name="ModeHybridRadio"
                                     Content="Hybrid (offline + online fallback) — set up in Settings"
                                     IsEnabled="False" />
                        <RadioButton x:Name="ModeOnlineRadio"
                                     Content="Online Gemini — set up in Settings"
                                     IsEnabled="False" />
                        <TextBlock Style="{StaticResource VayuMutedText}" TextWrapping="Wrap"
                                   Text="This dashboard sets up Local AI. Online and Hybrid AI are available — save a provider key in Settings → Online AI, then pick Online or Hybrid in Settings → AI Mode. Cloud calls ask for consent every time, and every plan still passes through the Permission Guard." />
                    </StackPanel>

                    <!-- 3. Ollama Status -->
                    <StackPanel x:Name="OllamaPanel" Spacing="10" Visibility="Collapsed">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10">
                                <Border Width="3" Height="14" Background="{StaticResource VayuAccentVioletBrush}" />
                                <TextBlock Style="{StaticResource VayuSectionText}" Text="OLLAMA STATUS" VerticalAlignment="Center" />
                            </StackPanel>
                            <Button Grid.Column="1" x:Name="OllamaRefreshButton" Content="Refresh" Click="OnOllamaRefreshClick" />
                        </Grid>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Style="{StaticResource VayuMutedText}" Text="Endpoint:" />
                            <TextBlock x:Name="OllamaEndpointText" Style="{StaticResource VayuBodyText}" Text="http://localhost:11434" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Style="{StaticResource VayuMutedText}" Text="Executable on PATH:" />
                            <TextBlock x:Name="OllamaExecutableText" Style="{StaticResource VayuBodyText}" Text="Probing…" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Style="{StaticResource VayuMutedText}" Text="Server reachable:" />
                            <TextBlock x:Name="OllamaServerText" Style="{StaticResource VayuBodyText}" Text="Probing…" />
                        </StackPanel>
                        <TextBlock x:Name="OllamaMessageText" Style="{StaticResource VayuMutedText}" TextWrapping="Wrap" Text="Vayu will NOT install Ollama automatically. Model downloads always ask for permission first." />
                    </StackPanel>

                    <!-- 4. Models -->
                    <StackPanel x:Name="ModelsPanel" Spacing="10" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Border Width="3" Height="14" Background="{StaticResource VayuAccentAmberBrush}" />
                            <TextBlock Style="{StaticResource VayuSectionText}" Text="MODEL CATALOG" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock x:Name="ModelsSummaryText" Style="{StaticResource VayuBodyText}" Text="Installed curated models: 0 / 3" />
                        <ItemsControl x:Name="ModelsItems">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="svc:LocalModelRow">
                                    <Border Margin="0,4,0,4" Padding="16,12,16,12" CornerRadius="8"
                                            Background="{StaticResource VayuGlassFill}"
                                            BorderBrush="{StaticResource VayuGlassStroke}" BorderThickness="1">
                                        <Grid ColumnSpacing="16">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <!-- Left: identity + metadata -->
                                            <StackPanel Grid.Column="0" Spacing="3">
                                                <StackPanel Orientation="Horizontal" Spacing="8">
                                                    <TextBlock Style="{StaticResource VayuBodyText}" FontSize="16" Text="{x:Bind DisplayName}" VerticalAlignment="Center" />
                                                    <TextBlock Style="{StaticResource VayuMutedText}" Text="{x:Bind ModelTag}" VerticalAlignment="Center" />
                                                </StackPanel>
                                                <TextBlock Style="{StaticResource VayuMutedText}" Text="{x:Bind Description}" TextWrapping="Wrap" />
                                                <TextBlock Style="{StaticResource VayuMutedText}" Text="{x:Bind HardwareNote}" TextWrapping="Wrap" />
                                                <!-- Installed metadata chips -->
                                                <StackPanel Orientation="Horizontal" Spacing="6" Visibility="{x:Bind MetadataVisibility, Mode=OneWay}">
                                                    <Border Style="{StaticResource VayuChipTeal}"><TextBlock Style="{StaticResource VayuChipText}" Text="{x:Bind DisplaySize, Mode=OneWay}" /></Border>
                                                    <Border Style="{StaticResource VayuChip}"><TextBlock Style="{StaticResource VayuChipText}" Text="{x:Bind ParameterSize, Mode=OneWay}" /></Border>
                                                    <Border Style="{StaticResource VayuChip}"><TextBlock Style="{StaticResource VayuChipText}" Text="{x:Bind FamilyLabel, Mode=OneWay}" /></Border>
                                                </StackPanel>
                                            </StackPanel>

                                            <!-- Right: status + action + progress -->
                                            <StackPanel Grid.Column="1" Spacing="8" MinWidth="190" VerticalAlignment="Center">
                                                <Border Style="{StaticResource VayuChip}" HorizontalAlignment="Right">
                                                    <TextBlock Style="{StaticResource VayuChipText}" Text="{x:Bind StatusText, Mode=OneWay}" />
                                                </Border>
                                                <Button Content="Download"
                                                        HorizontalAlignment="Stretch"
                                                        Click="OnDownloadRowClick"
                                                        Tag="{x:Bind ModelTag}"
                                                        Visibility="{x:Bind DownloadButtonVisibility, Mode=OneWay}"
                                                        IsEnabled="{x:Bind CanDownload, Mode=OneWay}" />
                                                <!-- Progress block: only while downloading -->
                                                <StackPanel Spacing="4" Visibility="{x:Bind ProgressVisibility, Mode=OneWay}">
                                                    <ProgressBar Minimum="0" Maximum="100"
                                                                 Value="{x:Bind ProgressPercent, Mode=OneWay}"
                                                                 IsIndeterminate="{x:Bind ProgressIsIndeterminate, Mode=OneWay}"
                                                                 Foreground="{StaticResource VayuAccentCyanBrush}" />
                                                    <Button Content="Cancel"
                                                            HorizontalAlignment="Stretch"
                                                            Click="OnCancelDownloadClick" />
                                                </StackPanel>
                                                <TextBlock Style="{StaticResource VayuMutedText}"
                                                           Text="{x:Bind StatusDetail, Mode=OneWay}"
                                                           Visibility="{x:Bind StatusDetailVisibility, Mode=OneWay}"
                                                           TextWrapping="Wrap"
                                                           HorizontalAlignment="Right" />
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Style="{StaticResource VayuMutedText}" TextWrapping="Wrap"
                                   Text="Click Download to pull a curated model via your local Ollama. Vayu shows the exact tag in a consent dialog before any bytes leave Ollama's HTTP API. Downloads can take time and disk space — Cancel is available per row." />
                    </StackPanel>

                    <!-- 5. Verify -->
                    <StackPanel x:Name="VerifyPanel" Spacing="10" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Border Width="3" Height="14" Background="{StaticResource VayuAccentCyanBrush}" />
                            <TextBlock Style="{StaticResource VayuSectionText}" Text="VERIFICATION" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock x:Name="VerifyHeadlineText" Style="{StaticResource VayuBodyText}" FontSize="18" Text="Probing…" TextWrapping="Wrap" />
                        <TextBlock x:Name="VerifyGuidanceText" Style="{StaticResource VayuMutedText}" TextWrapping="Wrap" Text="" />
                        <TextBlock Style="{StaticResource VayuMutedText}" TextWrapping="Wrap"
                                   Text="No installs or downloads happened without your permission. You can revisit the Setup Dashboard any time from Settings → Open Setup Dashboard." />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Status line / completed badge -->
            <TextBlock Grid.Row="2" x:Name="CompletedStatusText" Style="{StaticResource VayuMutedText}" Text="" />

            <!-- Footer buttons -->
            <Grid Grid.Row="3" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" x:Name="SkipButton" Content="Skip for now" Click="OnSkipClick" />
                <Button Grid.Column="2" x:Name="BackButton" Content="Back" Click="OnBackClick" IsEnabled="False" />
                <Button Grid.Column="3" x:Name="NextButton" Content="Continue" Click="OnNextClick" Style="{StaticResource AccentButtonStyle}" />
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
